General:
- Make sure dimension are good and consistent across different devices
- Some formatting for equations are still off. For example sd is labeled as "s_d" , "n_pairs" should have "pairs" as subscript. And there are just other general inconsistencies which can make it confusing
- ✅ ~~Tutor library should reset when you enter a new page, currently it's difficult to tell when you are looking at tips from a previous page~~
- Sometimes tutor tips appear down at the bottom of the page and you can't see them
- ✅ ~~For items that you can hover over to get more information about, make it so that the pop-up has a little delay (as in you have to hold your cursor over it for a short amount of time). At the moment, sometimes the pop-ups appear instantaneously, which can become annoying and overwhelming~~
- Double check that the Power pages operate properly


✅ ~~Measures of Variability - Mean, median, mode markers overlap on the distribution shape graph~~

NHST Page:
- ✅ ~~The p-value interactive visualizer shading for the tails doesn't map onto the normal curve line perfectly~~
- ✅ ~~Nothing actually changes when you select the different steps~~
- ✅ ~~Confidence interval visualizer is a bit difficult to grab~~
- ✅ ~~Application could probably use some guidance about what's going on when estimates and interval widths are adjusted for confidence intervals~~
- ✅ ~~Power visualizer is kinda small~~

One Sample t-Test:
- ✅ ~~On tutor/lesson page, there's no reason to have the "Errors/Power" button, it doesn't do anything to the visualizer~~
- ✅ ~~Step by step calculator button doesn't need to be thing on the tutor/lesson page, since that's more for the calculator~~
- ✅ ~~Tutor/lesson page doesn't need the equation box, since there's a tab for the equation~~
- ✅ ~~Make sure visualizer is on screen even as the screen is scrolled downwards~~
- ✅ ~~In the calculator, errors/power button only shows the power curves when "Hide Alternative" button is toggled. This isn't how it should be~~
- Errors/Power visualizer is a little noisy and the legend on the side is cut off and tiny
- ✅ ~~In the calculator, there's no way of using imported data like the other tests on the application, you can only paste in data~~
- Equation page is not interactive like all other equation pages
- ✅ ~~Power Analysis: adjusting alpha crashes the site~~

Independent Samples t-Test:
- ✅ ~~Visualizer should stay on page when you scroll down~~

Paired Samples t-Test:
- I think the paired visualizer should be a typical plot maker like the bar plot option, but paired can be an option as well as a line chart option (if this isn't already an option)

One-Way ANOVA:
- ✅ ~~I think the tutor/lesson page should default to "Explore" mode instead of "Compute" mode~~
- I know the means visualizer is basically this, but there should be an actual plot maker built in
- ✅ ~~The effect size page only covers cohen's f, it does mention Eta squared, but the layout is a little confusing, like it lists eta squared twice and it doesn't explain what eta's squared is, or how it is calculated~~
- ✅ ~~I'm thinking there should be a table view, like the one featured on the ANCOVA page, which explains what the table is telling you. You should also be able to hover over a cell of the table, and it explains what that cell indicates~~

ANCOVA:
- ✅ ~~Needs some kind of guidance and tutor for the tutor/lessons plot. It doesn't really explain what's going on when you are adjusting covariate~~
- ✅ ~~The tutor/lessons tab order should be edited around. I think the F-Dist should be first and should be combined with the data tab. Then, the table, then the explorer, then the plot~~

Factorial ANOVA:
- Many of the same things as the ANCOVA...
- ✅ ~~For the Data page, you should explain what is meant by themes, like give a brief description of what the study is testing, under each condition there should also be a brief description (e.g., "Factor B: Music" "Genre of music that participants listened to")~~
- Some themes should have pre-built additional levels for independent variables, so I can add another level of music (e.g., electronic, quiet, etc.)
- Maybe also make some themes pre-set to 3 factors. Currently, only two way interactions can be learned. So maybe just one three-way
- Should include an in-depth bar graph and plot maker, like in the other pages
- ✅ ~~Factorial ANOVA Equation page only shows interaction equation. It does say "Showing" so it seems that you should be able to change it, but you can't as far as I'm aware.~~

Repeated Measures ANOVA:
- Needs  the most work. The page is not currently updated to the current format, so make it match the other ANOVA pages. Make sure to match the style and also apply and changes that you see that I've had you make here for the others

Correlations:
- Maybe add the ability to make a correlation table and correlation matrix to evaluate multiple correlations at the same time. Maybe reserve this for the calculator

Regressions:
- ✅ ~~Assumptions are listed on the lesson/tutor page, which is not necessary since there is a page devoted towards that anyway~~
- ✅ ~~On the 3D plane plot, there is sometimes some overlap on scale labels~~
- ✅ ~~Same thing as the ANOVA table here with the regression tables, you should probably make it so that when you hover over a cell so that it tells you what it means~~
- ✅ ~~Assumptions also appear on the test calculator page. Just have them appear on the assumptions page~~







